{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Klapsenwanderung im Browser starten",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Klapsenwanderung Server starten"
    }
  ]
}

